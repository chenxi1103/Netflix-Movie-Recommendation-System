import os
from pathlib import Path
ROOT_DIR = str(Path(__file__).parent.parent)
INFERENCE_DIR = ROOT_DIR + '/inference'
INFERENCE_CONFIG_PATH = str(ROOT_DIR) +  '/inference/configuration/config.json'
TEST_INFERENCE_CONFIG_PATH = str(ROOT_DIR)+'test/resource/configuration/config.json'



